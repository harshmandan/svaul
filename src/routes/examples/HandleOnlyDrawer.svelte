<script lang="ts">
    import SheetExample from "./SheetExample.svelte";
</script>

<SheetExample
    name="Handle only"
    description="Only the handle drags it."
    heading="Handle only"
    explainer={"Only the handle at the top drags this drawer. The rest of the panel is free for taps and scrolling. Turn it on with `handleOnly`."}
    handleOnly
/>
